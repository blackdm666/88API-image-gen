# 更新说明 v2.0.3

## 失败后重新提交

- 将容易被理解为永久禁令的 `[NO-RETRY]` 改为 `[NO-AUTO-RETRY]`。
- 已受理或状态未知的请求仍不会被脚本自动重排、切换模型或补发，避免重复计费。
- 用户在看到风险提示后明确说“重试”“重新生成”“再试一次”时，视为授权一次新的付费请求；Codex 不再强制用户先查看 88API 日志。
- 兼容旧版本输出的 `[NO-RETRY]`，同样只禁止自动重试，不阻止用户明确发起新请求。
