# 更新说明 v0.2.1

`0.2.1` 将首次配置检查结果改为中文显示。

- `hasKey: true/false` 改为 `密钥状态: 已配置/未配置`。
- worker 数量、启用数量、上限和密钥列表均使用中文字段。
- `--list-workers` 的终端提示同步中文化。
- README 和 Skill 的自动检查规则同步使用中文状态字段。

本次更新不改变 API 地址、生图参数或 worker 调度行为。
